spring:
  profiles:
    active: virtual
  application:
    name: prx-backbone-rest
  cloud:
    config:
      profile: virtual
      label: developer
      uri: http://localhost:8082
      send-state: true
    inetutils:
      default-hostname: prx.test