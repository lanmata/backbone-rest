spring:
  profiles:
    active: virtual
  application:
    name: prx-backbone-rest
  cloud:
    config:
      profile: virtual
      label: developer
      uri: http://localhost:8082
    inetutils:
      default-hostname: prx.test