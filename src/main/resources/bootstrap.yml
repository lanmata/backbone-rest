spring:
  profiles:
    active: virtual
  application:
    name: prx-backbone-rest
  cloud:
    config:
      profile: virtual
      label: developer
      uri: prx.test/config-server
      send-state: true
  jpa:
    defer-datasource-initialization: true
