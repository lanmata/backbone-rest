spring:
  profiles:
    active: virtual
  application:
    name: prx-backbone-rest
  cloud:
    config:
      profile: virtual
      label: developer
      uri: qa.prx.test/config-server
      send-state: true
  jpa:
    defer-datasource-initialization: true
